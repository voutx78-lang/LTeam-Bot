const cards = [["payments", "Оплаты", "Проверка чеков и поступлений", "▣"], ["payouts", "Выплаты", "Заявки исполнителей на вывод", "↗"], ["disputes", "Споры", "Разбор проблемных сделок", "!"]];
export default function AdminWorkspace({ summary = {}, onNavigate, onOpenBot }) {
  const counts = { payments: summary.payments ?? 0, payouts: summary.payouts ?? 0, disputes: summary.disputes ?? 0 };
  return <section className="admin-workspace"><header className="admin-workspace-head"><button onClick={() => onNavigate("profile")}>←</button><div><p>LTEAM CONTROL</p><h1>Админ-панель</h1></div><span>ADMIN</span></header><section className="admin-brief"><div><b>{Object.values(counts).reduce((sum, value) => sum + Number(value || 0), 0)}</b><span>требуют внимания</span></div><p>Управляйте проверками и финансовыми действиями через защищённый интерфейс бота.</p></section><section className="admin-queue">{cards.map(([key, title, caption, icon]) => <button key={key} onClick={() => onOpenBot(key)}><i>{icon}</i><span><b>{title}</b><small>{caption}</small></span><strong>{counts[key]}</strong><em>›</em></button>)}</section><section className="admin-tools"><p>ДОПОЛНИТЕЛЬНО</p><button onClick={() => onOpenBot("admin_panel")}>Открыть полную админ-панель бота <span>→</span></button><small>Доступ ограничен администраторами, подтверждёнными сервером.</small></section></section>;
}
